---
import { Image } from 'astro:assets';
import globalSettings from '../../content/settings/global.json';
import homepage from '../../content/pages/homepage.json';
import cookieBanner from '../../content/pages/cookie-banner.json';
import { Instagram, Facebook, Phone, MessageCircle } from 'lucide-astro';
import logoFullDefault from '../assets/images/brand/logo-black-tagline.png';
import { cleanPhoneHref, whatsappHref } from '../lib/contact';

const brandImages = import.meta.glob<{ default: ImageMetadata }>(
  '/src/assets/images/brand/**/*.{png,jpg,jpeg,gif,webp,svg}',
  { eager: true }
);

const logoFull =
  (globalSettings.logoFooter && brandImages[globalSettings.logoFooter]?.default) ||
  logoFullDefault;

const currentYear = new Date().getFullYear();
const email = globalSettings.contact?.email || 'info@viralagency.it';
const locations = (homepage as any).locations ?? [];
const whatsappRaw = (globalSettings as any).whatsapp?.number || '';
const whatsappMessage = (globalSettings as any).whatsapp?.defaultMessage || '';
const globalWhatsappUrl = whatsappHref(whatsappRaw, whatsappMessage);
const footerData = (globalSettings as any).footer ?? {};
const footerEyebrow = footerData.eyebrow || 'Parliamone';
const footerTagline = footerData.tagline || 'Communication · Marketing · Social · Branding';
const footerSiteCredit = footerData.siteCredit || 'Site design — REAL GOES VIRAL';

const BASE = import.meta.env.BASE_URL;
const privacyHref = BASE + 'privacy-policy/';
const cookieHref = BASE + 'cookie-policy/';
const managePrefsLabel = (cookieBanner as any).managePrefsLabel || 'Gestisci cookie';

const mapsUrl = (loc: { city?: string; address?: string; mapsUrl?: string }) => {
  if (loc.mapsUrl) return loc.mapsUrl;
  const q = [loc.address, loc.city].filter(Boolean).join(', ');
  return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`;
};
const locationPhone = (loc: any) => loc.phone || globalSettings.contact?.phone || '';
const locationWhatsappUrl = (loc: any) =>
  whatsappHref(loc.whatsappNumber || whatsappRaw, loc.whatsappMessage || whatsappMessage);
---

<footer class="section-dark mt-0">
  <div class="container-custom py-16 md:py-24">
    <!-- Huge mailto -->
    <div class="mb-16 md:mb-24 reveal">
      <p class="eyebrow text-white/60">{footerEyebrow}</p>
      <a
        href={`mailto:${email}`}
        class="block h-display text-[clamp(2.5rem,9vw,7rem)] text-[var(--paper)] hover:text-[var(--electric)] transition-colors duration-500 leading-[0.9] mt-4 break-all"
        data-cursor="scrivici"
      >
        {email}
        <span class="inline-block align-top text-[var(--electric)] ml-3 text-[0.6em]">↗</span>
      </a>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-10 md:gap-8">
      <!-- Brand -->
      <div class="col-span-2">
        <p class="mono-label text-white/60 mb-3">Brand</p>
        <Image src={logoFull} alt={globalSettings.companyName} width={320} height={140} class="h-20 w-auto img-darken" />
        <p class="mono-label text-white/40 mt-4">
          {globalSettings.companyName} — {footerTagline}
        </p>
      </div>

      <!-- Contatti -->
      <div>
        <p class="mono-label text-white/60 mb-3">Contatti</p>
        <ul class="space-y-2 text-sm text-white/80">
          <li><a href={`mailto:${email}`} class="hover:text-[var(--electric)] transition-colors">{email}</a></li>
          {globalSettings.contact?.phone && (
            <li>
              <a href={`tel:${cleanPhoneHref(globalSettings.contact.phone)}`} class="inline-flex items-center gap-1.5 hover:text-[var(--electric)] transition-colors" aria-label={`Chiama ${globalSettings.contact.phone}`}>
                <Phone class="w-3.5 h-3.5 shrink-0" strokeWidth={1.8} />
                <span>{globalSettings.contact.phone}</span>
              </a>
            </li>
          )}
          {globalWhatsappUrl && (
            <li>
              <a href={globalWhatsappUrl} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-1.5 hover:text-[var(--electric)] transition-colors" aria-label="Scrivici su WhatsApp">
                <MessageCircle class="w-3.5 h-3.5 shrink-0" strokeWidth={1.8} />
                <span>WhatsApp</span>
              </a>
            </li>
          )}
        </ul>

        {locations.length > 0 && (
          <>
            <p class="mono-label text-white/60 mt-6 mb-3">Sedi</p>
            <ul class="space-y-3 text-sm text-white/80">
              {locations.map((loc: any) => (
                <li>
                  {loc.address ? (
                    <a
                      href={mapsUrl(loc)}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="group block hover:text-[var(--electric)] transition-colors"
                      data-cursor="apri mappa"
                      aria-label={`Apri ${loc.city ?? loc.address} su Google Maps`}
                    >
                      <span class="block text-[var(--paper)] group-hover:text-[var(--electric)] transition-colors">
                        {loc.city}{loc.tag && <span class="text-white/40"> — {loc.tag}</span>}
                      </span>
                      <span class="block text-white/60 group-hover:text-[var(--electric)] transition-colors">{loc.address}</span>
                    </a>
                  ) : (
                    <span class="block text-[var(--paper)]">
                      {loc.city}{loc.tag && <span class="text-white/40"> — {loc.tag}</span>}
                    </span>
                  )}
                  <span class="block mt-1 space-x-3">
                    {locationPhone(loc) && (
                      <a
                        href={`tel:${cleanPhoneHref(locationPhone(loc))}`}
                        class="inline-flex items-center gap-1.5 text-white/60 hover:text-[var(--electric)] transition-colors"
                        aria-label={`Chiama la sede di ${loc.city}`}
                      >
                        <Phone class="w-3.5 h-3.5 shrink-0" strokeWidth={1.8} />
                        <span>{locationPhone(loc)}</span>
                      </a>
                    )}
                    {locationWhatsappUrl(loc) && (
                      <a
                        href={locationWhatsappUrl(loc)}
                        target="_blank"
                        rel="noopener noreferrer"
                        class="inline-flex items-center gap-1.5 text-white/60 hover:text-[var(--electric)] transition-colors"
                        aria-label={`Scrivi alla sede di ${loc.city} su WhatsApp`}
                      >
                        <MessageCircle class="w-3.5 h-3.5 shrink-0" strokeWidth={1.8} />
                        <span>WhatsApp</span>
                      </a>
                    )}
                  </span>
                </li>
              ))}
            </ul>
          </>
        )}
      </div>

      <!-- Legale + social -->
      <div>
        <p class="mono-label text-white/60 mb-3">Legale</p>
        <ul class="space-y-2 text-sm text-white/60">
          {globalSettings.legal?.piva && <li>P.IVA {globalSettings.legal.piva}</li>}
          {globalSettings.legal?.codiceDestinatario && <li>SDI {globalSettings.legal.codiceDestinatario}</li>}
        </ul>

        <p class="mono-label text-white/60 mt-6 mb-3">Seguici</p>
        <div class="flex gap-2">
          {globalSettings.social?.instagram && (
            <a href={globalSettings.social.instagram} target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="w-10 h-10 rounded-full border border-white/30 grid place-items-center hover:bg-[var(--electric)] hover:border-[var(--electric)] transition-colors">
              <Instagram class="w-4 h-4" strokeWidth={1.5} />
            </a>
          )}
          {globalSettings.social?.facebook && (
            <a href={globalSettings.social.facebook} target="_blank" rel="noopener noreferrer" aria-label="Facebook" class="w-10 h-10 rounded-full border border-white/30 grid place-items-center hover:bg-[var(--electric)] hover:border-[var(--electric)] transition-colors">
              <Facebook class="w-4 h-4" strokeWidth={1.5} />
            </a>
          )}
          {globalWhatsappUrl && (
            <a href={globalWhatsappUrl} target="_blank" rel="noopener noreferrer" aria-label="WhatsApp" class="w-10 h-10 rounded-full border border-white/30 grid place-items-center hover:bg-[var(--electric)] hover:border-[var(--electric)] transition-colors">
              <MessageCircle class="w-4 h-4" strokeWidth={1.5} />
            </a>
          )}
          {(globalSettings.social as any)?.googleMapsUrl && (
            <a href={(globalSettings.social as any).googleMapsUrl} target="_blank" rel="noopener noreferrer" aria-label="Google Business Profile" class="w-10 h-10 rounded-full border border-white/30 grid place-items-center hover:bg-[var(--electric)] hover:border-[var(--electric)] transition-colors">
              <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/>
              </svg>
            </a>
          )}
        </div>
        {(globalSettings.social as any)?.googleReviewsUrl && (
          <a
            href={(globalSettings.social as any).googleReviewsUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-1.5 mt-4 text-xs text-white/50 hover:text-[var(--electric)] transition-colors mono-label"
          >
            <svg class="w-3 h-3 shrink-0" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"/></svg>
            Lascia una recensione su Google
          </a>
        )}
      </div>
    </div>

    <div class="mt-16 pt-8 border-t border-white/10 flex flex-wrap gap-4 items-center justify-between mono-label text-white/50">
      <span>© {currentYear} {globalSettings.companyName} — Tutti i diritti riservati</span>
      <div class="flex flex-wrap gap-4 items-center">
        <a href={privacyHref} class="hover:text-[var(--electric)] transition-colors">Privacy</a>
        <a href={cookieHref} class="hover:text-[var(--electric)] transition-colors">Cookie</a>
        <button type="button" id="footer-cookie-prefs" class="hover:text-[var(--electric)] transition-colors mono-label text-white/50 bg-transparent border-0 cursor-pointer p-0">
          {managePrefsLabel}
        </button>
        <span class="hidden md:inline">{footerSiteCredit} · {currentYear}</span>
      </div>
    </div>
  </div>
</footer>

<script is:inline>
  document.getElementById('footer-cookie-prefs')?.addEventListener('click', () => {
    if (typeof window.openCookiePreferences === 'function') {
      window.openCookiePreferences();
    }
  });
</script>
